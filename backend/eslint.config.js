import baseConfig from '../eslint.config';

export default {
  ...baseConfig,
  // rules: {
  //   ...baseConfig.rules,
  //   'no-console': 'error', // Например, отключаем `no-console` в серверной части
  // },
};
